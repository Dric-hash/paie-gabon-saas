flask==3.0.0
flask-sqlalchemy==3.1.1
flask-login==0.6.3
flask-mail==0.10.0
flask-wtf==1.2.1
werkzeug==3.0.1
sqlalchemy==2.0.23
openpyxl==3.1.2
python-dotenv==1.0.0
gunicorn==21.2.0
psycopg2-binary==2.9.9
reportlab==4.4.10
# Cache partagé multi-workers (optionnel — désactivé si REDIS_URL non définie)
redis==5.0.1
# Compression Gzip automatique des réponses HTTP (réduit la taille de 60-80%)
flask-compress==1.14
flask-limiter==3.5.0
# Sauvegarde automatique vers Backblaze B2 (API compatible S3)
boto3==1.34.69
# Monitoring des erreurs en production (alertes + stack traces)
sentry-sdk[flask]==2.61.1
